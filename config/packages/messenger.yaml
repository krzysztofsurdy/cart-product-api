framework:
    messenger:
        buses:
            messenger.bus.command.product: ~
            messenger.bus.query.product: ~
            messenger.bus.event.product: ~

            messenger.bus.command.cart: ~
            messenger.bus.query.cart: ~

        transports:
            sync: 'sync://'

        default_bus: messenger.bus.command.product
